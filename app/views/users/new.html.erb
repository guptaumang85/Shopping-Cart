<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<%= nested_form_for :user, url: signup_path, html: { multipart: true } do |f| %>

  <%= render(:partial => "form", :locals => {:f => f}) %>

  <%= f.submit :"Sign Up" %>

<% end %>