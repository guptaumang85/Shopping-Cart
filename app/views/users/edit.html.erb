<% provide(:title, "Edit user") %>
<h1>Update your profile</h1>

<%= nested_form_for :user, url: user_edit_path, html: { multipart: true } do |f| %>
<%= render(:partial => "form", :locals => {:f => f}) %>

  <%= f.submit "Save changes" %>

<% end %>
